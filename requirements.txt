# =============================================================================
# requirements.txt -- Python dependencies for the artwork-db project
# =============================================================================

# --- Extraction (Met OpenAccess loader) ---
snowflake-connector-python>=3.7.0
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# --- dbt Diagnostics Tool (lineage-aware error tracer) ---
sqlglot>=26.0
dbt-artifacts-parser>=0.13.0
pyyaml>=6.0
