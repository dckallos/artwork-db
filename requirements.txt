# =============================================================================
# requirements.txt -- Python dependencies for the artwork-db project
# =============================================================================

# --- Extraction (Met OpenAccess loader) ---
snowflake-connector-python>=3.7.0
requests>=2.31.0
aiohttp>=3.9.0
python-dotenv>=1.0.0

# --- dbt ---
dbt-core>=1.10.0,<2.0
dbt-snowflake>=1.10.0,<2.0

# --- dbt Diagnostics Tool (sibling repo) ---
# Install from the sibling clone:
#   pip install -e ../dbt-diagnostics[dev]

