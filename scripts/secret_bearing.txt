# SQL scripts that render a secret (e.g. the GitHub PAT) inline and must
# have their Snowflake CLI stdout suppressed during apply. One path per
# line, relative to repo root. Blank lines and #-comments are ignored.
# Fail-closed contract (see scripts/bootstrap.py::is_secret_bearing):
#   - any script listed here is ALWAYS applied with stdout suppressed;
#   - any script NOT listed here that contains the github_pat marker
#     aborts the run instead of risking a cleartext echo.
git-setup/create_git_ops_db.sql