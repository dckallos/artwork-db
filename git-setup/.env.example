# git-setup/.env -- gitignored. Copy from this example and fill in.
# Sourced (set -a; source git-setup/.env; set +a) before make iac so
# snow sql injects the PAT via -D "github_pat=${GITHUB_PAT}".
# NEVER commit a populated .env. The real PAT lives only here, locally.

GITHUB_PAT=